# Smart Helmet Backend

FastAPI backend for the Smart Helmet IoT monitoring system.

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
uvicorn app.main:app --reload
```

## Environment

Copy `.env.example` to `.env` and update the values.
